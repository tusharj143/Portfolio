/**
 * Shared layout constants.
 * Single source of truth for values used across multiple components.
 */

/** Height of the hero scroll zone in viewport height units.
 *  Used by ScrollyCanvas (container height) and Overlay (absolute overlay height).
 *  Change this ONE value to adjust the entire hero scroll duration. */
export const HERO_HEIGHT_VH = 500;
